- content_for :page_title do
  Nieuwe speeldagen
%p Kies een methode om een of meer nieuwe datums te maken.
%fieldset
  %legend Kies een specifieke datum.
  = form_tag action: 'new' do
    = render partial: 'form'
    = submit_tag 'Opslaan'
%fieldset
  %legend Voeg een reeks toe.
  = form_tag action: 'create' do
    %p
      %label{ for: 'daytype' } Soort dag:
      = select_tag 'daytype', daytype_options(@daytype)
    %p
      %label{ for: 'period' } Tot einde van:
      = select_tag 'period', period_options(@period)
    = submit_tag 'Reeks maken'
= link_to 'Terug naar lijst', action: 'index'
